# Fashion-MNIST-CNN-Classification
Classifies Fashion-MNIST clothing images into 10 categories using a CNN built with TensorFlow/Keras. Architecture includes two Conv2D layers, MaxPooling, Dense layers, and Dropout regularization. Trained for 20 epochs achieving 91.40% test accuracy, with overfitting analysis across training history.
